name: My First Pipeline

on:
  workflow_dispatch:

jobs:

  hello:

    runs-on: ubuntu-latest

    steps:

      - name: Print Message

        run: echo "Hello Dost"

      - name: Show Current Folder

        run: pwd

      - name: Show Files

        run: ls